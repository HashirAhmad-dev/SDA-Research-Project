# Copy to `.env` and fill in. `.env` is gitignored.
#
# Branch 3 of the PII pipeline calls Qwen2.5-VL-7B-Instruct through Hugging
# Face Inference Providers (an OpenAI-compatible endpoint at
# https://router.huggingface.co/v1). Create a token with "Make calls to
# Inference Providers" permission at https://huggingface.co/settings/tokens
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

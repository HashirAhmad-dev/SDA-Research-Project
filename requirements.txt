fastapi>=0.115
uvicorn[standard]>=0.30
streamlit>=1.39
pandas>=2.2
numpy>=2.1
plotly>=5.24
pydantic>=2.9
requests>=2.32

# Hybrid anomaly engine (backend/anomaly_engine.py) and the evaluation harness.
# torch is required at inference time: the LSTM is a real trained model, not a
# replayed table. See evaluation/REAL_RESULTS.md.
torch>=2.2
scikit-learn>=1.4

# evaluation/generate_sessions.py only.
faker>=25.0
